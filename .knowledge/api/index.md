# Public API

The public surface is **upstream's**, not ours. This section is a thin orientation map so a reader knows what
the two products expose and where to look — it is not a reimplementation of the upstream reference docs.

# Concepts

* [public-surface.md](public-surface.md) - `StytchCore` / `StytchUI` products, the client entry points, and the generated concurrency variants
