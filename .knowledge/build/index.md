# Build & release

How the fork is built, tested, versioned, and kept in sync with upstream. Tooling and workflows are inherited
from upstream — the fork-specific part is the **upstream-sync + branch-pin** model.

# Concepts

* [ci-and-release.md](ci-and-release.md) - Makefile targets, CI workflows, Sourcery codegen, `cut_version` versioning, and keeping the fork synced
